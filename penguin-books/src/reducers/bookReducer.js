export const initialState = {
    quote:'',
    character:'',
}

export const bookReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}